<x-app-layout><div class="sb-page"><div class="sb-wrap"><div class="sb-heading"><div><span class="sb-kicker">EMPLOYEE DASHBOARD</span><h1>Welcome back, {{ explode(' ', auth()->user()->name)[0] }} <span>✦</span></h1><p>Reservations, pickups, and returns for today.</p></div><a class="sb-btn" href="{{ route('employee.catalog') }}">Browse collection</a></div><div class="sb-stats"><div class="sb-stat"><span>Today's reservations</span><b>{{ $today }}</b><small>Pickups scheduled today</small><i>◷</i></div><div class="sb-stat"><span>Pending reservations</span><b>{{ $pending }}</b><small>Awaiting confirmation</small><i>✧</i></div><div class="sb-stat"><span>Active rentals</span><b>{{ $rentals }}</b><small>Currently with customers</small><i>♡</i></div><div class="sb-stat"><span>Gowns available</span><b>{{ $available }}</b><small>Ready for a new booking</small><i>♧</i></div></div><div class="sb-columns"><section class="sb-panel"><div class="sb-panel-head"><div><h2>Reservation activity</h2><p>Recent bookings to take care of</p></div><span class="sb-pill">Latest</span></div><div class="sb-table-wrap"><table class="sb-table"><thead><tr><th>BOOKING</th><th>CUSTOMER</th><th>DATES</th><th>STATUS</th></tr></thead><tbody>@forelse($reservations as $booking)<tr><td><strong>{{ $booking->reservation_code }}</strong><small class="sb-cell-sub">{{ $booking->items->first()?->gown?->name ?? 'Gown reservation' }}</small></td><td>{{ $booking->customer->full_name ?? 'Customer' }}</td><td>{{ $booking->pickup_date?->format('M d') }} – {{ $booking->return_date?->format('M d') }}</td><td><span class="sb-status">{{ ucfirst(str_replace('_',' ',$booking->status)) }}</span></td></tr>@empty<tr><td colspan="4" class="sb-empty">No reservations yet.</td></tr>@endforelse</tbody></table></div></section><section class="sb-panel sb-sidepanel"><div class="sb-panel-head"><div><h2>Upcoming returns</h2><p>Scheduled returns</p></div></div>@forelse($returns as $booking)<div class="sb-return"><span class="sb-date-chip">{{ $booking->return_date?->format('d') }}<small>{{ $booking->return_date?->format('M') }}</small></span><div><b>{{ $booking->customer->full_name ?? 'Customer' }}</b><small>Reservation {{ $booking->reservation_code }}</small></div></div>@empty<div class="sb-empty">No returns coming up.</div>@endforelse<div class="sb-tip"><span>♡</span><div><b>Today’s reminder</b><p>Check the reservation details before each pickup.</p></div></div></section></div></div></div></x-app-layout>
