<x-app-layout><div class="sb-page"><div class="sb-wrap"><div class="sb-heading"><div><span class="sb-kicker">RESERVATION REQUEST</span><h1>Request a <em>gown.</em></h1><p>Share a few details and our team will confirm your booking.</p></div><a class="sb-outline-btn" href="{{ route('customer.catalog') }}">← Back to collection</a></div><div class="sb-columns"><section class="sb-panel sb-form-panel"><form method="POST" action="{{ route('customer.reserve.store', $gown) }}" class="sb-reservation-form">@csrf<h2>Your booking details</h2><p class="sb-form-intro">We’ll use these details to coordinate your fitting and rental.</p><label>Full name<input value="{{ auth()->user()->name }}" disabled></label><label>Email address<input value="{{ auth()->user()->email }}" disabled></label><label>Contact number<input name="contact_number" required value="{{ old('contact_number') }}" placeholder="09XX XXX XXXX">@error('contact_number')<small>{{ $message }}</small>@enderror</label><div class="sb-form-row"><label>Pickup date<input type="date" name="pickup_date" required min="{{ today()->format('Y-m-d') }}" value="{{ old('pickup_date') }}">@error('pickup_date')<small>{{ $message }}</small>@enderror</label><label>Return date<input type="date" name="return_date" required min="{{ today()->format('Y-m-d') }}" value="{{ old('return_date') }}">@error('return_date')<small>{{ $message }}</small>@enderror</label></div><label>Occasion<select name="event_type" required><option value="">Choose your occasion</option><option>Wedding</option><option>Prom</option><option>Gala</option><option>Debut</option><option>Other special event</option></select></label><label>Notes for our team<textarea name="notes" rows="3" placeholder="Anything you’d like us to know?">{{ old('notes') }}</textarea></label><button class="sb-btn" type="submit">Submit reservation request →</button></form></section><aside class="sb-panel sb-reserve-summary"><span class="sb-kicker">YOUR SELECTION</span><div class="sb-summary-image" @if($gown->image) style="background-image:url('{{ asset('storage/'.$gown->image) }}')" @endif>✿</div><small>{{ $gown->category->name ?? 'THE COLLECTION' }} · SIZE {{ $gown->size ?? 'VARIOUS' }}</small><h2>{{ $gown->name }}</h2><div class="sb-summary-line"><span>Rental fee</span><b>₱{{ number_format($gown->rental_price, 2) }}</b></div><div class="sb-summary-line"><span>Security deposit</span><b>₱{{ number_format($gown->security_deposit, 2) }}</b></div><div class="sb-summary-line sb-summary-total"><span>Total due</span><b>₱{{ number_format($gown->rental_price + $gown->security_deposit, 2) }}</b></div><p class="sb-summary-hint">Payment is collected after the team confirms your reservation. Your request will appear as pending.</p></aside></div></div></div></x-app-layout>
