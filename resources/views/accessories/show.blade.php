<x-app-layout><div class="sb-page sb-inventory-page"><div class="sb-wrap">
    <div class="sb-heading"><div><span class="sb-kicker">INVENTORY · ACCESSORIES</span><h1>{{ $accessory->name }}</h1><p>Accessory stock record and linked gown looks.</p></div><div class="sb-inventory-actions"><a href="{{ route('owner.accessories.edit',$accessory) }}">Edit item</a><a href="{{ route('owner.accessories.index') }}">Back to inventory</a></div></div>
    <div class="sb-inventory-note"><span>✧</span><div><b>Why accessories are tracked separately</b>Each accessory is a stock item with its own quantity and replacement cost. It can be linked to one or more gowns from the gown edit form, so staff know which pieces come with a look.</div></div>
    <section class="sb-panel sb-inventory-form-card"><h2>Stock details</h2><div class="sb-customer-booking-bottom"><div><small>QUANTITY</small><b>{{ $accessory->quantity }}</b></div><div><small>REPLACEMENT COST</small><b>₱{{ number_format($accessory->replacement_cost,2) }}</b></div><div><small>STATUS</small><b>{{ ucfirst($accessory->status) }}</b></div></div><p>{{ $accessory->description ?: 'No description provided.' }}</p><h3>Linked gowns</h3>@forelse($accessory->gowns as $gown)<div class="sb-team-row"><div class="sb-team-copy"><b>{{ $gown->name }}</b><small>{{ $gown->gown_code }} · {{ $gown->category->name ?? 'Uncategorized' }} · linked quantity {{ $gown->pivot->quantity }}</small></div><a class="sb-text-link" href="{{ route('owner.gowns.show',$gown) }}">View gown</a></div>@empty<p>No gowns are linked to this accessory. Edit a gown and select this item under Included accessories.</p>@endforelse</section>
</div></div></x-app-layout>
