<x-app-layout><div class="sb-page"><div class="sb-wrap"><div class="sb-heading"><div><span class="sb-kicker">TEAM & ACCESS</span><h1>Your <em>team.</em></h1><p>Create staff accounts and manage employee access to operations.</p></div><a class="sb-btn" href="{{ route('owner.dashboard') }}">← Dashboard</a></div>@if(session('success'))<div class="sb-success">{{ session('success') }}</div>@endif<div class="sb-columns sb-employee-columns"><section class="sb-panel sb-form-panel"><form method="POST" action="{{ route('owner.employees.store') }}" class="sb-reservation-form">@csrf<h2>Add an employee</h2><p class="sb-form-intro">A new employee account receives operational access only.</p><label>Full name<input name="name" required value="{{ old('name') }}"></label><label>Email address<input type="email" name="email" required value="{{ old('email') }}"></label><label>Temporary password<input type="password" name="password" minlength="8" required></label><label>Contact number<input name="contact_number" value="{{ old('contact_number') }}"></label><label>Position<input name="position" value="{{ old('position','Rental Staff') }}" required></label><button class="sb-btn" type="submit">Create employee account</button></form></section><section class="sb-panel"><div class="sb-panel-head"><div><h2>Staff directory</h2><p>{{ $employees->total() }} employee accounts</p></div></div><div class="sb-team-list">@forelse($employees as $employee)<div class="sb-team-row"><div class="sb-avatar">{{ strtoupper(substr($employee->full_name,0,1)) }}</div><div class="sb-team-copy"><b>{{ $employee->full_name }}</b><small>{{ $employee->position }} · {{ $employee->employee_code }}<br>{{ $employee->user->email }}</small></div><details class="sb-employee-edit"><summary>Edit details</summary><form method="POST" action="{{ route('owner.employees.update', $employee) }}">@csrf @method('PUT')<label>Name<input name="name" value="{{ $employee->full_name }}" required></label><label>Email<input type="email" name="email" value="{{ $employee->user->email }}" required></label><label>Phone<input name="contact_number" value="{{ $employee->contact_number }}"></label><label>Position<input name="position" value="{{ $employee->position }}" required></label><button class="sb-small-btn">Save details</button></form></details><form method="POST" action="{{ route('owner.employees.toggle', $employee) }}">@csrf @method('PATCH')<button class="sb-small-btn {{ $employee->status==='active'?'sb-deactivate':'' }}">{{ $employee->status==='active'?'Deactivate':'Activate' }}</button></form></div>@empty<div class="sb-empty">No employees yet. Create the first staff account.</div>@endforelse</div></section></div><div class="sb-pagination">{{ $employees->links() }}</div></div></div></x-app-layout>
